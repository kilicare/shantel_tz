"use client";

import { FormEvent, useEffect, useState } from "react";
import { AlertCircle, Check, Pencil, Plus, RefreshCw, Trash2 } from "lucide-react";
import { WorkspaceNavigation } from "@/components/WorkspaceNavigation";
import { apiClient } from "@/lib/api-client";

type Kind = "customers" | "suppliers" | "locations";
type Item = { id: string; name: string; email?: string; phone?: string; code?: string; status?: string; balance?: number | string };
const settings = { customers: { title: "Customers", singular: "Customer", endpoint: "/customers" }, suppliers: { title: "Suppliers", singular: "Supplier", endpoint: "/suppliers" }, locations: { title: "Locations", singular: "Location", endpoint: "/locations" } } as const;
function unwrap(response: any) { const payload = response?.data?.data ?? response?.data ?? response; return payload?.data ?? payload ?? []; }

export function EditableMasterWorkspace({ kind }: { kind: Kind }) {
  const current = settings[kind];
  const [items, setItems] = useState<Item[]>([]);
  const [form, setForm] = useState({ name: "", email: "", phone: "", code: "", address: "", customerType: "INDIVIDUAL", locationType: "MAIN_STORE" });
  const [editingId, setEditingId] = useState("");
  const [permissions, setPermissions] = useState<string[]>([]);
  const [search, setSearch] = useState("");
  const [showForm, setShowForm] = useState(false);
  const [error, setError] = useState("");
  const [message, setMessage] = useState("");
  const [loading, setLoading] = useState(true);
  async function load() { try { setLoading(true); setError(""); const records = unwrap(await apiClient.get(`${current.endpoint}?page=1&limit=100`)); if (kind === "customers") { const withBalances = await Promise.all(records.map(async (item: Item) => ({ ...item, balance: (await apiClient.get(`/customers/${item.id}/balance`)).data?.data?.totalBalance ?? 0 }))); setItems(withBalances); } else if (kind === "suppliers") { const withBalances = await Promise.all(records.map(async (item: Item) => ({ ...item, balance: (await apiClient.get(`/suppliers/${item.id}/balance`)).data?.data?.totalBalance ?? 0 }))); setItems(withBalances); } else setItems(records); } catch (requestError: any) { const value = requestError?.response?.data?.message; setError(Array.isArray(value) ? value[0] : value || `${current.title} could not be loaded.`); } finally { setLoading(false); } }
  useEffect(() => { try { setPermissions(JSON.parse(localStorage.getItem("shantel_user") ?? "null")?.permissions ?? []); } catch { setPermissions([]); } void load(); }, [kind]);
  function edit(item: Item) { setEditingId(item.id); setShowForm(true); setForm({ ...form, name: item.name, email: item.email ?? "", phone: item.phone ?? "", code: item.code ?? "" }); }
  async function save(event: FormEvent) { event.preventDefault(); try { const body = kind === "locations" ? { name: form.name, code: form.code, address: form.address, locationType: form.locationType } : { name: form.name, email: form.email || undefined, phone: form.phone || undefined }; const response = editingId ? await apiClient.patch(`${current.endpoint}/${editingId}`, body) : await apiClient.post(current.endpoint, kind === "locations" ? body : { ...body, customerType: form.customerType }); const saved = unwrap(response); setMessage(`${saved.name} ${editingId ? "updated" : "created"} successfully.`); setForm({ name: "", email: "", phone: "", code: "", address: "", customerType: "INDIVIDUAL", locationType: "MAIN_STORE" }); setEditingId(""); setShowForm(false); await load(); } catch (requestError: any) { const value = requestError?.response?.data?.message; setError(Array.isArray(value) ? value[0] : value || `${current.singular} could not be saved.`); } }
  async function deactivate(item: Item) { try { await apiClient.patch(`${current.endpoint}/${item.id}/deactivate`); setMessage(`${item.name} deactivated successfully.`); await load(); } catch (requestError: any) { const value = requestError?.response?.data?.message; setError(Array.isArray(value) ? value[0] : value || `${item.name} could not be deactivated.`); } }
  const filtered = items.filter((item) => JSON.stringify(item).toLowerCase().includes(search.toLowerCase()));
  const canEdit = permissions.includes(`${kind}.edit`); const canCreate = permissions.includes(`${kind}.create`);
  return <><WorkspaceNavigation /><main className="min-h-screen bg-[#f4f1ec] px-4 py-5 text-[#17221f] sm:px-6 sm:py-8 lg:px-8"><div className="mx-auto max-w-7xl"><header className="flex flex-col justify-between gap-5 border-b border-[#17221f]/12 pb-7 sm:flex-row sm:items-end"><div><p className="text-xs font-semibold uppercase tracking-[0.24em] text-[#ad6742]">Master data</p><h1 className="mt-2 text-4xl font-semibold tracking-[-0.05em]">{current.title}</h1></div><div className="flex gap-2"><button type="button" onClick={() => void load()} className="flex items-center gap-2 border border-[#17221f]/15 px-4 py-2.5 text-xs font-semibold uppercase tracking-[0.12em]"><RefreshCw size={15} /> Refresh</button>{canCreate && <button type="button" onClick={() => { setEditingId(""); setShowForm(!showForm); }} className="flex items-center gap-2 bg-[#17221f] px-4 py-2.5 text-xs font-semibold uppercase tracking-[0.1em] text-white"><Plus size={15} /> New {current.singular}</button>}</div></header>{error && <div role="alert" className="mt-6 flex items-center gap-3 border border-[#ad6742]/30 bg-[#ad6742]/8 px-4 py-3 text-sm text-[#8a4931]"><AlertCircle size={18} /> {error}</div>}{message && <div role="status" className="mt-6 flex items-center gap-2 text-sm text-[#365b4a]"><Check size={16} /> {message}</div>}{showForm && <form onSubmit={save} className="mt-8 grid gap-4 bg-[#17221f] p-6 text-[#f4f1ec] sm:grid-cols-2"><h2 className="text-xl font-semibold sm:col-span-2">{editingId ? `Edit ${current.singular}` : `New ${current.singular}`}</h2><input required aria-label={`${current.singular} name`} placeholder="Name" value={form.name} onChange={(event) => setForm({ ...form, name: event.target.value })} className="h-11 border-b border-white/25 bg-transparent text-sm outline-none" />{kind === "locations" ? <><input required aria-label="Location code" placeholder="Code" value={form.code} onChange={(event) => setForm({ ...form, code: event.target.value })} className="h-11 border-b border-white/25 bg-transparent text-sm outline-none" /><select aria-label="Location type" value={form.locationType} onChange={(event) => setForm({ ...form, locationType: event.target.value })} className="h-11 bg-[#f4f1ec] px-2 text-sm text-[#17221f]"><option value="MAIN_STORE">Main store</option><option value="BRANCH">Branch</option><option value="PROJECT_STORE">Project store</option><option value="OTHER">Other</option></select></> : <><input type="email" aria-label="Email" placeholder="Email" value={form.email} onChange={(event) => setForm({ ...form, email: event.target.value })} className="h-11 border-b border-white/25 bg-transparent text-sm outline-none" /><input aria-label="Phone" placeholder="Phone" value={form.phone} onChange={(event) => setForm({ ...form, phone: event.target.value })} className="h-11 border-b border-white/25 bg-transparent text-sm outline-none" /></>}<input aria-label="Address" placeholder="Address" value={form.address} onChange={(event) => setForm({ ...form, address: event.target.value })} className="h-11 border-b border-white/25 bg-transparent text-sm outline-none" /><button type="submit" className="bg-[#e8a36b] px-4 py-2.5 text-xs font-semibold text-[#17221f] sm:col-span-2">{editingId ? "Save changes" : `Create ${current.singular.toLowerCase()}`}</button></form>}<section className="mt-8 bg-white p-6"><div className="flex items-end justify-between"><h2 className="text-2xl font-semibold">{current.title}</h2><span className="text-xs text-[#17221f]/45">{filtered.length} records</span></div><input aria-label={`Search ${current.title}`} value={search} onChange={(event) => setSearch(event.target.value)} placeholder={`Search ${current.title.toLowerCase()}`} className="mt-5 h-11 w-full border-b border-[#17221f]/15 bg-transparent text-sm outline-none" />{loading ? <p className="py-10 text-sm text-[#17221f]/50">Loading...</p> : <div className="mt-4 divide-y divide-[#17221f]/10">{filtered.map((item) => <div key={item.id} className="flex items-center justify-between py-4"><div><p className="text-sm font-semibold">{item.name}{item.code ? ` · ${item.code}` : ""}</p><p className="text-xs text-[#17221f]/45">{item.email ?? item.phone ?? item.status ?? "ACTIVE"}{item.balance !== undefined ? ` · Balance: TSh ${item.balance}` : ""}</p></div>{canEdit && <div className="flex gap-1"><button type="button" aria-label={`Edit ${item.name}`} onClick={() => edit(item)} className="p-2"><Pencil size={15} /></button><button type="button" aria-label={`Deactivate ${item.name}`} onClick={() => void deactivate(item)} className="p-2 text-[#ad6742]"><Trash2 size={15} /></button></div>}</div>)}</div>}</section></div></main></>;
}
