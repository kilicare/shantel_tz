"use client";

type Option = { label: string; value: string };
export function FormField({ label, type = "text", placeholder, value, onChange, error, required, disabled, options, className = "", helpText }: { label: string; type?: "text" | "email" | "password" | "number" | "date" | "textarea" | "select"; placeholder?: string; value: string | number; onChange: (value: string) => void; error?: string | null; required?: boolean; disabled?: boolean; options?: Option[]; className?: string; helpText?: string }) {
  const classes = `min-h-9 w-full rounded-lg border bg-background px-3 text-sm outline-none focus:ring-2 focus:ring-ring ${error ? "border-destructive" : ""}`;
  return <div className={`space-y-1.5 ${className}`}><label className="text-sm font-medium">{label}{required ? <span className="text-destructive"> *</span> : null}</label>{type === "textarea" ? <textarea className={`${classes} min-h-24 py-2`} placeholder={placeholder} value={value} onChange={(event: React.ChangeEvent<HTMLTextAreaElement>) => onChange(event.target.value)} disabled={disabled} /> : type === "select" ? <select className={classes} value={value} onChange={(event: React.ChangeEvent<HTMLSelectElement>) => onChange(event.target.value)} disabled={disabled}><option value="">{placeholder ?? "Select"}</option>{options?.map((option) => <option value={option.value} key={option.value}>{option.label}</option>)}</select> : <input className={`${classes} ${error ? "border-destructive" : ""}`} type={type} placeholder={placeholder} value={value} onChange={(event: React.ChangeEvent<HTMLInputElement>) => onChange(event.target.value)} disabled={disabled} />}{error ? <p className="text-xs text-destructive">{error}</p> : helpText ? <p className="text-xs text-muted-foreground">{helpText}</p> : null}</div>;
}
